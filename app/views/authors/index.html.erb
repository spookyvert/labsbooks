<h1> Authors!</h1>
<%= link_to "New Author", new_author_url(@author) %><br>

<ul>
  <% @authors.each do |a| %>
  <li><%= link_to a.name, author_path(a)%> (b. <%= a.birth_year %>)</li>
  <% end %>
</ul>
